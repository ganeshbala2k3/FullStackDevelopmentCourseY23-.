import React from "react"; //es6 modules

//every component returns only single element
//every component should start with capital letter


const First =()=>{
    return(
        <div>
            <h1>I am your first component</h1>
        </div>
    );
}

export default First //es6 modules